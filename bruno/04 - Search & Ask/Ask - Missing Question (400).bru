meta {
  name: Ask - Missing Question (400)
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/api/v1/document/ask
  body: json
  auth: inherit
}

headers {
  Content-Type: application/json
}

body:json {
  {}
}

settings {
  encodeUrl: true
  timeout: 0
}

tests {
  test("rejects a blank question", function () {
    expect(res.getStatus()).to.equal(400);
    expect(res.body.detail).to.equal("Question is mandatory");
  });
}

docs {
  Fails fast, before the async LLM call is even scheduled.
}
