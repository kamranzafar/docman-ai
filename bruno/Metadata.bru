meta {
  name: Metadata
  type: http
  seq: 4
}

get {
  url: http://localhost:8081/document/metadata/:id
  body: none
  auth: inherit
}

params:path {
  id: f102f673-dc48-46d7-9f50-a2526bf80992
}

settings {
  encodeUrl: true
  timeout: 0
}

docs {
  Returns the latest metadata by default. Append a version number to look up a specific
  past version's snapshot instead, e.g. `/document/metadata/:id/1` - 404s if that version
  doesn't exist.
}
