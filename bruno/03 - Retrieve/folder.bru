meta {
  name: 03 - Retrieve
  seq: 3
}

docs {
  Metadata and content lookups, latest and version-specific. All requests target
  `{{documentId}}` - run `01 - Create` and, ideally, `02 - Update` first so at least two
  versions exist to compare against each other.
}
